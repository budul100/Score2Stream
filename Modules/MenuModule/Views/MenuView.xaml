<UserControl
    x:Class="MenuModule.Views.MenuView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:fl="clr-namespace:Fluent;assembly=Fluent"
    xmlns:hl="clr-namespace:MenuModule.Helpers"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:vm="clr-namespace:MenuModule.ViewModels"
    d:DataContext="{Binding Source={x:Static hl:DesignTimeViewModelLocator.MenuViewModel}}"
    d:DesignWidth="500"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary Source="pack://application:,,,/Fluent;Component/Themes/Generic.xaml" />
    </UserControl.Resources>
    <DockPanel>
        <fl:Ribbon
            AutomaticStateManagement="True"
            IsAutomaticCollapseEnabled="False"
            IsCollapsed="False"
            IsQuickAccessToolBarMenuDropDownVisible="False"
            IsQuickAccessToolBarVisible="False"
            SelectedTabIndex="{Binding SelectedTabIndex, Mode=TwoWay}">
            <fl:RibbonTabItem Header="Webcam">
                <fl:RibbonGroupBox Header="Webcam control">
                    <fl:Button Command="{Binding WebcamPlayCommand}" Header="Play" />
                    <fl:Button Command="{Binding WebcamPauseCommand}" Header="Pause" />
                </fl:RibbonGroupBox>
            </fl:RibbonTabItem>
            <fl:RibbonTabItem Header="Clips">
                <fl:RibbonGroupBox Header="Clips selection">
                    <fl:Button Command="{Binding ClipAddCommand}" Header="Add" />
                    <fl:Button Command="{Binding ClipRemoveCommand}" Header="Remove" />
                </fl:RibbonGroupBox>
            </fl:RibbonTabItem>
            <fl:RibbonTabItem Header="Templates">
                <fl:RibbonGroupBox Header="Templates selection">
                    <fl:DropDownButton Header="Select" ItemsSource="{Binding Templates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="vm:TemplateViewModel">
                                <fl:MenuItem
                                    Command="{Binding OnClickCommand}"
                                    Header="{Binding Name}"
                                    IsChecked="{Binding IsActive, Mode=TwoWay}"
                                    Size="Small" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </fl:DropDownButton>
                    <fl:Button Command="{Binding TemplateRemoveCommand}" Header="Remove" />
                </fl:RibbonGroupBox>
                <fl:RibbonGroupBox Header="Samples detecion">
                    <fl:ToggleButton Header="Determine samples" />
                </fl:RibbonGroupBox>
            </fl:RibbonTabItem>
        </fl:Ribbon>
    </DockPanel>
</UserControl>