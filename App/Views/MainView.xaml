<Window
    x:Class="ScoreboardOCR.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:ScoreboardOCR.Core;assembly=Core"
    xmlns:prism="http://prismlibrary.com/"
    Title="{Binding Title}"
    Width="{Binding Width, Mode=TwoWay}"
    Height="{Binding Height, Mode=TwoWay}"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel LastChildFill="True">
        <ContentControl prism:RegionManager.RegionName="{x:Static core:RegionNames.MenuRegion}" DockPanel.Dock="Top" />
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <ContentControl Grid.Column="0" prism:RegionManager.RegionName="{x:Static core:RegionNames.WebcamRegion}" />
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />
            <ContentControl Grid.Column="2" prism:RegionManager.RegionName="{x:Static core:RegionNames.ClipRegion}" />
        </Grid>
    </DockPanel>
</Window>